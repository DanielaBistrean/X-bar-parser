#ifndef GRAMMAR_H
#define GRAMMAR_H 1

#include <string>

extern bool isVerb(std::string word);
extern bool isNoun(std::string word);
extern bool isAdj(std::string word);
extern bool isAdv(std::string word);
extern bool isDet(std::string word);
extern bool isPrep(std::string word);
extern bool isComp(std::string word);

#endif
